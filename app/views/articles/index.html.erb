<div class="d-flex align-items-center">
  <h1>DELI Database</h1>
  <div class="ml-auto articles__linkBox">
    <%= form_with url: articles_path, method: :get, class: 'articles__searchForm' do %>
      <%= select_tag :tag_id,
                      options_from_collection_for_select(Tag.all, :id, :name, params[:tag_id]),
                      {
                        prompt: '種類で絞り込む',
                        class: 'form-control articles__select',
                        onchange: 'submit(this.form)'
                      }
       %>
    <% end %>
    <%= link_to '新規投稿', new_article_path, class: 'btn btn-outline-dark' %>
  </div>
</div>

<% if flash[:notice] %>
  <div class="alert alert-danger">
    <%= flash[:notice] %>
  </div>
<% end %>

<%= paginate @articles %>
<table class="table table-hover articles__table">
  <thead class="thead-dark">
    <tr>
      <th>タイトル</th>
      <th>内容</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @articles.each do |article| %>
    <tr>
      <td>
        <%= article.title %>
      </td>
      <td>
        <%= article.body.truncate(10) %>
      </td>
      <td>
        <%= link_to '詳細', article, class: 'btn btn-outline-dark' %>
      </td>
      <td>
        <%= link_to '削除', article, class: 'btn btn-outline-dark', method: :delete %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @articles %>